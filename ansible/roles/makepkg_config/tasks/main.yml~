---
- name: create yay cache
  file:
    path: /var/cache/makepkg
    state: directory
    mode: 'u=rwx,g=rwx,o=rwxt'

- name: make packages in /var/cache/makepkg instead of local dir
  lineinfile:
    path: /etc/makepkg.conf
    regexp: '^.*PKGDEST=.*'
    line: 'PKGDEST=/var/cache/makepkg'

    